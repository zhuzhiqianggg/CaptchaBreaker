# 技术架构文档

## 目录
- [1. 系统架构](#1-系统架构)
- [2. 核心组件](#2-核心组件)
- [3. 数据流](#3-数据流)
- [4. 关键技术实现](#4-关键技术实现)
- [5. 技术选型说明](#5-技术选型说明)

---

## 1. 系统架构

### 1.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                            客户端 (Client)                            │
│               Browser / Python Script / Mobile App                   │
└────────────────────────────────┬─────────────────────────────────────┘
                                 │ HTTP/HTTPS
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      FastAPI Web Server (app/main.py)                │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                      路由层 (Routes)                            │  │
│  │  POST /ocr/upload  │  POST /ocr/base64  │  POST /ocr/url       │  │
│  └───────────┬────────┴──────────┬─────────┴──────────┬───────────┘  │
│              │                   │                    │               │
│              └───────────────────┴────────────────────┘               │
│                                 │                                     │
│              ┌──────────────────▼──────────────────┐                 │
│              │       图像预处理 (Preprocessing)      │                 │
│              │  ┌────────────────────────────────┐ │                 │
│              │  │ 1. RGB转换 + 格式标准化          │ │                 │
│              │  │ 2. 智能上采样 (小图自动放大)      │ │                 │
│              │  │ 3. 对比度增强 (1.5x)             │ │                 │
│              │  └────────────────────────────────┘ │                 │
│              └──────────────────┬──────────────────┘                 │
│                                 │                                     │
│              ┌──────────────────▼──────────────────┐                 │
│              │       PaddleOCR 引擎                  │                 │
│              │  ┌────────────────────────────────┐ │                 │
│              │  │ 文本检测: PP-OCRv5_server_det  │ │                 │
│              │  │ 文本识别: en_PP-OCRv5_mobile   │ │                 │
│              │  │ (支持中英文双语模型)              │ │                 │
│              │  └────────────────────────────────┘ │                 │
│              └──────────────────┬──────────────────┘                 │
│                                 │                                     │
│              ┌──────────────────▼──────────────────┐                 │
│              │       结果解析 (Result Parsing)       │                 │
│              │  - 文本提取 + 空格清理                │                 │
│              │  - 置信度提取                        │                 │
│              │  - 边界框坐标转换                     │                 │
│              └──────────────────┬──────────────────┘                 │
│                                 │                                     │
│              ┌──────────────────▼──────────────────┐                 │
│              │       JSON Response                   │                 │
│              │  {success, texts, full_text, ...}    │                 │
│              └─────────────────────────────────────┘                 │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 模块关系

```
app/main.py
├── FastAPI App (lifespan, routes)
├── PaddleOCR Models (global, lazy-loaded)
├── Preprocessing Pipeline
├── Result Parser
└── API Endpoints
    ├── POST /ocr/upload
    ├── POST /ocr/base64
    ├── POST /ocr/url
    ├── GET /
    └── GET /health
```

---

## 2. 核心组件

### 2.1 PaddleOCR 引擎

#### 模型架构
PaddleOCR 使用两阶段检测+识别架构：

```
输入图片 → 文本检测(Detector) → 裁剪文本区域 → 文本识别(Recognizer) → 输出文本
```

#### 使用的模型

| 模型 | 用途 | 类型 | 大小 |
|------|------|------|------|
| `PP-OCRv5_server_det` | 文本区域检测 | 检测器 | ~88MB |
| `PP-OCRv5_server_rec` | 中文文本识别 | 识别器 | ~12MB |
| `en_PP-OCRv5_mobile_rec` | 英文文本识别 | 识别器 | ~10MB |
| `PP-LCNet_x1_0_textline_ori` | 文本行方向分类 | 分类器 | ~5MB |

#### 模型存储位置
```
~/.paddlex/official_models/
├── PP-OCRv5_server_det/
├── PP-OCRv5_server_rec/
├── en_PP-OCRv5_mobile_rec/
├── PP-LCNet_x1_0_textline_ori/
├── PP-LCNet_x1_0_doc_ori/
└── UVDoc/
```

### 2.2 图像预处理管道

#### v5.0 当前策略

```
原始图片 → RGB转换 → 智能上采样 → 灰度化 → 对比度增强(1.5x) → 输出
```

| 步骤 | 作用 | 参数 |
|------|------|------|
| RGB转换 | 统一图片格式 | - |
| 智能上采样 | 小图片放大到200x60以上 | 自适应缩放 |
| 灰度化 | 消除颜色干扰 | PIL `.convert("L")` |
| 对比度增强 | 使字符与背景差异更明显 | 增强因子: 1.5 |

#### 预处理演进历史

| 版本 | 策略 | 准确率 | 问题 |
|------|------|--------|------|
| v4.0 | 过度预处理(二值化+连通域+形态学) | 0% | 破坏了文字信息 |
| v5.0 | 轻量预处理(上采样+对比度增强) | ~60% | 平衡了效果与稳定性 |

### 2.3 结果解析器

```python
def parse_ocr_result(result) -> Tuple[List[OCRResult], str]:
    # 1. 提取识别文本列表
    # 2. 提取置信度列表
    # 3. 提取边界框坐标
    # 4. 合并文本，去除空格
    # 5. 返回结构化结果
```

**输出格式**:
```json
{
    "success": true,
    "texts": [
        {"text": "A7k9", "confidence": 0.9956, "bounding_box": {...}}
    ],
    "full_text": "A7k9"
}
```

---

## 3. 数据流

### 3.1 上传识别流程

```
客户端
  │
  ├─ 1. POST /ocr/upload (multipart/form-data)
  │   ├─ file: captcha.png
  │   └─ language: "en"
  │
  ▼
FastAPI 服务器
  │
  ├─ 2. 保存上传文件到 temp/
  │
  ├─ 3. 预处理图像
  │   ├─ RGB转换
  │   ├─ 上采样 (if small)
  │   └─ 对比度增强
  │
  ├─ 4. 调用 PaddleOCR.ocr()
  │   ├─ 文本检测 → 找到文字区域
  │   └─ 文本识别 → 识别文字内容
  │
  ├─ 5. 解析结果
  │   ├─ 提取文本、置信度、边界框
  │   └─ 合并文本，清理空格
  │
  ├─ 6. 清理临时文件
  │
  └─ 7. 返回 JSON Response
         ├─ success: true
         ├─ full_text: "A7k9"
         ├─ texts: [...]
         └─ preprocessing_applied: [...]
```

### 3.2 内存管理

| 资源 | 管理方式 |
|------|----------|
| 上传文件 | 保存到 `temp/` 目录，处理完成后删除 |
| PaddleOCR 模型 | 启动时全局加载，常驻内存 |
| 处理中间图片 | 临时保存后立即删除 |
| API 请求 | 异步处理，自动释放 |

---

## 4. 关键技术实现

### 4.1 智能上采样

```python
def preprocess_image(image):
    width, height = image.size
    if width < 200 or height < 60:
        scale = max(200/width, 60/height)
        new_w = int(width * scale)
        new_h = int(height * scale)
        image = image.resize((new_w, new_h), Image.LANCZOS)
```

**原理**: 验证码图片通常很小(~100x40)，直接识别时字符像素太少。放大后每个字符有更多像素，OCR 模型能更准确地识别。

### 4.2 对比度增强

```python
img_gray = image.convert("L")
enhancer = ImageEnhance.Contrast(img_gray)
img_enhanced = enhancer.enhance(1.5)
```

**原理**: 增强字符与背景的对比度差异，使 OCR 检测器更容易区分文字区域和背景区域。

### 4.3 PaddleOCR 结果解析

```python
# PaddleOCR 3.x 返回 OCRResult 对象
ocr_data = result[0]
rec_texts = ocr_data.rec_texts    # ['A', '7', 'k', '9']
rec_scores = ocr_data.rec_scores  # [0.99, 0.98, 0.97, 0.99]
rec_polys = ocr_data.rec_polys    # 边界框坐标列表

# 合并结果
full_text = "".join(rec_texts)    # "A7k9"
```

---

## 5. 技术选型说明

### 5.1 为什么选择 PaddleOCR？

| 对比项 | PaddleOCR | Tesseract | EasyOCR |
|--------|-----------|-----------|---------|
| 中文识别 | ★★★★★ | ★★★☆☆ | ★★★★☆ |
| 英文识别 | ★★★★☆ | ★★★★★ | ★★★★☆ |
| 验证码识别 | ★★★★☆ | ★★☆☆☆ | ★★★☆☆ |
| 安装难度 | 中等 | 简单 | 简单 |
| 模型大小 | ~500MB | ~1GB | ~800MB |
| 社区支持 | 活跃(百度) | 成熟 | 一般 |
| API友好度 | 高 | 中 | 高 |

**选择理由**:
1. 对中英文混合识别效果最好
2. PP-OCRv5 是最新版本，针对小文字优化
3. 支持端到端推理，无需额外训练
4. 活跃的社区和文档

### 5.2 为什么选择 FastAPI？

| 对比项 | FastAPI | Flask | Django |
|--------|---------|-------|--------|
| 性能 | ★★★★★ | ★★★☆☆ | ★★☆☆☆ |
| 异步支持 | ★★★★★ | ★★★☆☆ | ★★★★☆ |
| 自动文档 | ★★★★★ | ★★★☆☆ | ★★★☆☆ |
| 类型检查 | ★★★★★ | ★★★☆☆ | ★★★☆☆ |
| 学习曲线 | 低 | 低 | 高 |

**选择理由**:
1. 自动生成交互式 API 文档 (Swagger UI)
2. 原生异步支持，高并发性能好
3. Pydantic 模型验证，类型安全
4. 现代 Python 框架，易于维护

### 5.3 图像预处理库选择

| 库 | 用途 | 选择理由 |
|----|------|----------|
| **Pillow** | 基础图像处理 | Python 标准图像库，简单易用 |
| **OpenCV** | 高级图像处理 | 强大的二值化、形态学操作 |
| **NumPy** | 数值计算 | 矩阵操作支持，与 OpenCV 配合 |

---

## 6. 性能指标

### 6.1 当前性能 (v5.0)

| 指标 | 数值 | 说明 |
|------|------|------|
| 真实准确率 | ~60% | 以文件名为基准的完全匹配 |
| 平均置信度 | ~0.85 | OCR 模型自身评估 |
| 平均延迟 | ~3.5s | CPU 模式，首次推理 |
| 内存占用 | ~1.5GB | 模型加载后常驻 |
| 模型大小 | ~500MB | 所有模型文件 |

### 6.2 性能瓶颈分析

| 环节 | 耗时 | 占比 | 优化空间 |
|------|------|------|----------|
| 模型加载 | ~30s (一次性) | - | GPU 加速可减半 |
| 图像预处理 | <0.1s | 3% | 已足够快 |
| OCR 推理 | ~3.4s | 97% | GPU 可提升 5-10x |
| 结果解析 | <0.01s | <1% | 已足够快 |

### 6.3 预期提升方向

| 优化方案 | 预期准确率 | 预期延迟 | 工作量 |
|----------|-----------|----------|--------|
| 模型 Fine-tune | 90-95% | 不变 | 中等 |
| GPU 加速 | 不变 | 0.3-0.7s | 低 (需硬件) |
| 多策略集成 | 75-85% | 增加 2-4x | 中等 |
| 定制预处理 | 70-80% | 不变 | 低 |

---

## 7. 部署架构

### 7.1 本地开发

```
run.py → app.main → FastAPI → PaddleOCR
端口: 8000
模式: 单进程
```

### 7.2 生产部署 (推荐)

```
Nginx → Gunicorn (多进程) → Uvicorn Worker → FastAPI
端口: 8000 → 80/443
模式: 4 workers
```

### 7.3 Docker 部署 (未来)

```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
